# QuickDrop Handoff — 2026-03-08

## 현재 상태
nginx 리버스 프록시 적용 완료. `http://46.250.251.82/`로 접속 (포트 80, 8200 직접 접근 차단). 비밀번호 변경됨. cleanup cron 등록됨.

## 작업 내용

### VPS 비밀번호 변경
- `/opt/envs/quickdrop.env`의 `QUICKDROP_PASSWORD`를 기본값(`changeme`)에서 실제 비밀번호로 변경
- `quickdrop.service` 재시작 → active (running) 확인

### cleanup.py cron 등록
- 매일 UTC 03:00 만료 파일 자동 정리
- crontab: `0 3 * * * cd /opt/apps/quickdrop/backend && /opt/apps/quickdrop/.venv/bin/python cleanup.py >> /opt/logs/quickdrop/cleanup.log 2>&1`
- 로그 디렉토리: `/opt/logs/quickdrop/`

### nginx 리버스 프록시 (Stage 4)
- `http://46.250.251.82/` → proxy to `127.0.0.1:8200`
- `client_max_body_size 500M`, `proxy_read/send_timeout 600s`
- ufw 8200/tcp 외부 차단 → nginx(80/443)만 통과

## 핵심 결정 & 맥락
- **접속 URL 변경**: `IP:8200` → `IP:80` (nginx 프록시). CLI 클라이언트도 URL 업데이트 필요
- **도메인 구매 후 전환 예정**: `drop.도메인`으로 서브도메인 전환 + HTTPS 적용

## 운영 명령어

```bash
sudo systemctl status/restart quickdrop
sudo systemctl status/restart nginx
sudo journalctl -u quickdrop -f
sudo nginx -t  # 설정 검증
sudo systemctl reload nginx  # 설정 반영

# 코드 업데이트
cd /opt/apps/quickdrop && git pull && sudo systemctl restart quickdrop

# nginx 설정 위치
/etc/nginx/sites-available/services
/etc/nginx/sites-enabled/services
```

## 다음 작업
- [ ] 도메인 구매 후 서브도메인 nginx 설정 전환
- [ ] certbot HTTPS 적용
- [ ] CLI 클라이언트 서버 URL 업데이트 (IP:8200 → 도메인)
- [ ] CI/CD (git push → 자동 배포)

## 세션 컨텍스트
- **대화 주제**: VPS 비밀번호 변경 → cron 등록 → nginx 리버스 프록시 설정
- **마지막 작업**: nginx 리버스 프록시 완료, 8200 포트 차단
